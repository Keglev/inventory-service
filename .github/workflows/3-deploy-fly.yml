# =============================================================================
# Smart Supply Pro - Fly.io Production Deployment
#
# Enterprise production deployment pipeline:
# - Purpose: Deploy pre-built, security-scanned Docker image to Fly.io.
# - Trigger: Manual workflow_dispatch (controlled rollout).
# - Design: Uses existing Docker image from registry (no rebuild).
# - Security: Requires FLY_API_TOKEN secret with appropriate scopes.
#
# Prerequisites:
#   1. Docker image must exist in registry (built by 1-ci-test.yml)
#   2. Image must have passed Trivy security scan
#   3. FLY_API_TOKEN secret configured in GitHub repository
#   4. Oracle Wallet secrets configured in Fly.io (ORACLE_WALLET_B64, etc.)
#
# Deployment workflow:
#   1-ci-test.yml → builds & scans image
#   2-deploy-ghpages.yml → publishes documentation
#   3-deploy-fly.yml (this file) → deploys to production (automatic after CI)
# =============================================================================

on:
  workflow_run:
    workflows: ["1️⃣ Backend CI - Build & Test"]
    types:
      - completed
    branches:
      - main
  workflow_dispatch:
    inputs:
      image_tag:
        description: 'Docker image tag to deploy (commit SHA or "latest")'
        required: true
        default: 'latest'
        type: string
      deployment_strategy:
        description: 'Deployment strategy'
        required: true
        default: 'immediate'
        type: choice
        options:
          - immediate
          - canary
          - rolling

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only run if the upstream workflow succeeded
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    environment:
      name: production
      url: https://inventoryservice.fly.dev
    
    steps:
      # ---------------------------------------------------------------------
      # 1) Set deployment variables (manual or automatic)
      # ---------------------------------------------------------------------
      - name: 📋 Set deployment variables
        id: vars
        run: |
          # Use manual input if workflow_dispatch, otherwise use commit SHA from upstream workflow
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            IMAGE_TAG="${{ inputs.image_tag }}"
            STRATEGY="${{ inputs.deployment_strategy }}"
          else
            IMAGE_TAG="${{ github.event.workflow_run.head_sha }}"
            STRATEGY="immediate"
          fi
          
          echo "image_tag=${IMAGE_TAG}" >> $GITHUB_OUTPUT
          echo "strategy=${STRATEGY}" >> $GITHUB_OUTPUT
          echo "📦 Deploying image: ckbuzin/inventory-service:${IMAGE_TAG}"
          echo "🎯 Strategy: ${STRATEGY}"
      # ---------------------------------------------------------------------
      # 2) Checkout repository (for fly.toml context)
      # ---------------------------------------------------------------------
      - name: Checkout code
        uses: actions/checkout@v4

      # ---------------------------------------------------------------------
      # 3) Validate Docker image exists
      # ---------------------------------------------------------------------
      - name: Validate Docker image
        run: |
          IMAGE_NAME="${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ steps.vars.outputs.image_tag }}"
          echo "Validating image: $IMAGE_NAME"
          
          # Login to Docker Hub to verify image
          echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
          
          if docker manifest inspect "$IMAGE_NAME" > /dev/null 2>&1; then
            echo "✅ Image found: $IMAGE_NAME"
            docker manifest inspect "$IMAGE_NAME" | jq -r '.manifests[0].digest'
          else
            echo "❌ ERROR: Image not found in registry: $IMAGE_NAME"
            exit 1
          fi
      # ---------------------------------------------------------------------
      # 3) Install Fly CLI
      # ---------------------------------------------------------------------
      - name: Install Fly CLI
        uses: superfly/flyctl-actions/setup-flyctl@master

      # ---------------------------------------------------------------------
      # 4) Verify Fly.io app exists
      # ---------------------------------------------------------------------
      - name: Verify Fly app
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          echo "Verifying Fly.io app: inventoryservice"
          if flyctl status --app inventoryservice > /dev/null 2>&1; then
            echo "✅ App exists"
            flyctl status --app inventoryservice
          else
            echo "❌ ERROR: Fly.io app 'inventoryservice' not found"
            exit 1
          fi
      # ---------------------------------------------------------------------
      # 5) Deploy to Fly.io
      # ---------------------------------------------------------------------
      - name: Deploy to Fly.io
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          IMAGE_NAME="${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ steps.vars.outputs.image_tag }}"
          
          echo "🚀 Deploying to Fly.io..."
          echo "  App: inventoryservice"
          echo "  Image: $IMAGE_NAME"
          echo "  Strategy: ${{ steps.vars.outputs.strategy }}"
          
          flyctl deploy \
            --app inventoryservice \
            --image "$IMAGE_NAME" \
            --strategy "${{ steps.vars.outputs.strategy }}" \
            --remote-only \
            --wait-timeout 300
      # ---------------------------------------------------------------------
      # 6) Health check
      # ---------------------------------------------------------------------
      - name: Health check
        run: |
          echo "Waiting for deployment to be healthy..."
          sleep 15
          
          HEALTH_URL="https://inventoryservice.fly.dev/health"
          echo "Checking: $HEALTH_URL"
          
          MAX_ATTEMPTS=12
          ATTEMPT=1
          
          while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
            echo "Attempt $ATTEMPT/$MAX_ATTEMPTS..."
            
            if curl -f -s -o /dev/null -w "%{http_code}" "$HEALTH_URL" | grep -q "200"; then
              echo "✅ Health check passed!"
              curl -s "$HEALTH_URL" | jq -r '.' || echo "Response received (non-JSON)"
              break
            else
              if [ $ATTEMPT -eq $MAX_ATTEMPTS ]; then
                echo "❌ Health check failed after $MAX_ATTEMPTS attempts"
                exit 1
              fi
              echo "Waiting 10 seconds before retry..."
              sleep 10
              ATTEMPT=$((ATTEMPT + 1))
            fi
          done
      # ---------------------------------------------------------------------
      # 7) Verify deployment
      # ---------------------------------------------------------------------
      - name: Verify deployment
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          echo "Fetching deployment status..."
          flyctl status --app inventoryservice
          
          echo ""
          echo "Recent logs (last 50 lines):"
          flyctl logs --app inventoryservice -n 50 || echo "⚠️ Could not fetch logs"
      # ---------------------------------------------------------------------
      # 8) Smoke tests
      # ---------------------------------------------------------------------
      - name: Smoke tests
        run: |
          BASE_URL="https://inventoryservice.fly.dev"
          
          echo "Running smoke tests against: $BASE_URL"
          
          # Test 1: Health endpoint
          echo "Test 1: Health endpoint"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$BASE_URL/health")
          if [ "$HTTP_CODE" = "200" ]; then
            echo "  ✅ Health check: PASS"
          else
            echo "  ❌ Health check: FAIL (HTTP $HTTP_CODE)"
            exit 1
          fi
          
          # Test 2: API root (should return 401 or redirect, not 500)
          echo "Test 2: API root"
          HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "$BASE_URL/")
          if [ "$HTTP_CODE" = "200" ] || [ "$HTTP_CODE" = "401" ] || [ "$HTTP_CODE" = "302" ]; then
            echo "  ✅ API root: PASS (HTTP $HTTP_CODE)"
          else
            echo "  ❌ API root: FAIL (HTTP $HTTP_CODE)"
            exit 1
          fi
          
          echo ""
          echo "✅ All smoke tests passed!"
      # ---------------------------------------------------------------------
      # 9) Summary
      # ---------------------------------------------------------------------
      - name: Job Summary
        if: always()
        run: |
          echo "## 🚀 Production Deployment" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **App:** \`inventoryservice\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image:** \`${{ secrets.DOCKER_USERNAME }}/inventory-service:${{ steps.vars.outputs.image_tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Strategy:** \`${{ steps.vars.outputs.strategy }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **URL:** https://inventoryservice.fly.dev" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Quick Links" >> $GITHUB_STEP_SUMMARY
          echo "- [Health Check](https://inventoryservice.fly.dev/health)" >> $GITHUB_STEP_SUMMARY
          echo "- [Fly.io Dashboard](https://fly.io/apps/inventoryservice)" >> $GITHUB_STEP_SUMMARY