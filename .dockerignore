# =============================================================================
# Smart Supply Pro — Monorepo Docker Ignore (inventory-service/.dockerignore)
#
# Enterprise guidance:
# - Keep the Docker build context minimal and free of local secrets.
# - Do NOT exclude files that the Dockerfiles explicitly COPY (e.g., `.mvn/`, `ops/nginx/`).
# - Avoid ignoring frontend or ops files since multi-stage builds may require them
#   when the build context is repo root.
# =============================================================================

# -----------------------------------------------------------------------------
# VCS / repo metadata
# -----------------------------------------------------------------------------
.git
.gitignore
.github/

# -----------------------------------------------------------------------------
# IDE & local tooling
# -----------------------------------------------------------------------------
.vscode
.idea
*.iml
*.classpath
*.project
.settings/

# -----------------------------------------------------------------------------
# OS cruft
# -----------------------------------------------------------------------------
**/.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# Environment files & local secrets (NEVER bake secrets into images)
# -----------------------------------------------------------------------------
.env
.env.*.local

# -----------------------------------------------------------------------------
# Backend: build outputs & logs
# -----------------------------------------------------------------------------
target/
logs/
log/
**/logs/
**/log/
*.log
out/
test-output/

# -----------------------------------------------------------------------------
# Test source code (NEVER include in production images)
# -----------------------------------------------------------------------------
src/test/

# -----------------------------------------------------------------------------
# Documentation & coverage reports (GitHub Pages only, never in Docker)
# -----------------------------------------------------------------------------
docs/
*.md
README.md
HELP.md

# -----------------------------------------------------------------------------
# Backend: Maven wrapper — KEEP `.mvn/`, ignore wrapper launchers
# Reason: Dockerfile COPY includes `.mvn/` to improve caching; don't ignore it.
# -----------------------------------------------------------------------------
mvnw
mvnw.cmd
# .mvn/   <-- DO NOT ignore this; required by Dockerfile COPY

# -----------------------------------------------------------------------------
# Oracle Wallet & related sensitive artifacts (kept out of context)
# -----------------------------------------------------------------------------
wallet/
oracle_wallet/
wallet.zip
*.zip
*.pem
tnsnames.ora
sqlnet.ora
cwallet.sso
ewallet.p12

# -----------------------------------------------------------------------------
# Frontend: cache/artifacts we don't need as source
# (The frontend image rebuilds `node_modules` and `dist` inside Docker.)
# -----------------------------------------------------------------------------
frontend/dist
frontend/coverage
frontend/cert

# -----------------------------------------------------------------------------
# Node caches ANYWHERE in the tree (frontend or otherwise)
# -----------------------------------------------------------------------------
**/node_modules
**/npm-debug.log
npm-debug.log*
yarn-error.log*

# -----------------------------------------------------------------------------
# Local compose & misc (not needed inside images)
# -----------------------------------------------------------------------------
docker-compose.yml
