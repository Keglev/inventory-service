# =============================================================================
# Smart Supply Pro — Monorepo Docker Ignore (inventory-service/.dockerignore)
#
# Purpose
# - Keep Docker build context small, fast, and free of secrets.
# - Work for BOTH backend and frontend images when context is `inventory-service/`.
# - Avoid ignoring files that the Dockerfiles explicitly COPY (e.g., .mvn/, ops/nginx/).
#
# Important
# - Do NOT add `frontend/**` or `ops/**` to ignores; the frontend image needs them.
# - We intentionally keep `.mvn/` included because the backend Dockerfile COPYs it.
# =============================================================================

# -----------------------------------------------------------------------------
# VCS / repo metadata
# -----------------------------------------------------------------------------
.git
.gitignore

# -----------------------------------------------------------------------------
# IDE & local tooling
# -----------------------------------------------------------------------------
.vscode
.idea
*.iml
*.classpath
*.project
.settings/

# -----------------------------------------------------------------------------
# OS cruft
# -----------------------------------------------------------------------------
**/.DS_Store
Thumbs.db

# -----------------------------------------------------------------------------
# Environment files & local secrets (NEVER bake secrets into images)
# -----------------------------------------------------------------------------
.env
.env.*.local

# -----------------------------------------------------------------------------
# Backend: build outputs & logs
# -----------------------------------------------------------------------------
target/
logs/
log/
**/logs/
**/log/
*.log
out/
test-output/

# -----------------------------------------------------------------------------
# Documentation & coverage reports (GitHub Pages only, never in Docker)
# -----------------------------------------------------------------------------
docs/

# -----------------------------------------------------------------------------
# Backend: Maven wrapper — KEEP `.mvn/`, ignore wrapper launchers
# Reason: Dockerfile COPY includes `.mvn/` to improve caching; don't ignore it.
# -----------------------------------------------------------------------------
mvnw
mvnw.cmd
# .mvn/   <-- DO NOT ignore this; required by Dockerfile COPY

# -----------------------------------------------------------------------------
# Oracle Wallet & related sensitive artifacts (kept out of context)
# -----------------------------------------------------------------------------
wallet/
oracle_wallet/
wallet.zip
*.zip
*.pem
tnsnames.ora
sqlnet.ora
cwallet.sso
ewallet.p12

# -----------------------------------------------------------------------------
# Frontend: cache/artifacts we don't need as source
# (The frontend image rebuilds `node_modules` and `dist` inside Docker.)
# -----------------------------------------------------------------------------
frontend/dist
frontend/coverage
frontend/cert

# -----------------------------------------------------------------------------
# Node caches ANYWHERE in the tree (frontend or otherwise)
# -----------------------------------------------------------------------------
**/node_modules
**/npm-debug.log
npm-debug.log*
yarn-error.log*

# -----------------------------------------------------------------------------
# Local compose & misc (not needed inside images)
# -----------------------------------------------------------------------------
docker-compose.yml
