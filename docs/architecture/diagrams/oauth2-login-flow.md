```markdown
# OAuth2 Login Flow

Placeholder: authentication sequence for OAuth2 (authorization request → provider → callback → user provisioning).

TODO: add Mermaid sequence diagram and notes on secure cookie usage.

```
