securitySchemes:
  oidc:
    type: openIdConnect
    openIdConnectUrl: https://accounts.google.com/.well-known/openid-configuration
  sessionCookie:
    type: apiKey
    in: cookie
    name: JSESSIONID   # change if you use a different cookie name or SESSION if that’s what your server sets

schemas:
  StockValueOverTimeDTO:
    $ref: ./schemas/StockValueOverTimeDTO.yaml
  StockPerSupplierDTO:
    $ref: ./schemas/StockPerSupplierDTO.yaml
  FinancialSummaryDTO:
    $ref: ./schemas/FinancialSummaryDTO.yaml
  PriceTrendDTO:
    $ref: ./schemas/PriceTrendDTO.yaml
  MonthlyStockMovementDTO:
    $ref: ./schemas/MonthlyStockMovementDTO.yaml
  StockUpdateFilterDTO:
    $ref: ./schemas/StockUpdateFilterDTO.yaml
  StockUpdateResultDTO:
    $ref: ./schemas/StockUpdateResultDTO.yaml
  DashboardSummaryDTO:
    $ref: ./schemas/DashboardSummaryDTO.yaml
  LowStockItemDTO:
    $ref: ./schemas/LowStockItemDTO.yaml
  ItemUpdateFrequencyDTO:
    $ref: ./schemas/ItemUpdateFrequencyDTO.yaml
  InventoryItemDTO:
    $ref: ./schemas/InventoryItemDTO.yaml
  StockChangeReason:
    $ref: ./schemas/StockChangeReason.yaml

parameters:
  Page:
    $ref: ./parameters/Page.yaml
  Size:
    $ref: ./parameters/Size.yaml
  Sort:
    $ref: ./parameters/Sort.yaml
