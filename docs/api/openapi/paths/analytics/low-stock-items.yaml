get:
  tags: [Analytics]
  summary: Low stock items (by supplier)
  operationId: getLowStockItems
  description: Items below their configured minimum threshold for the specified supplier.
  parameters:
    - in: query
      name: supplierId
      required: true
      schema: { type: string }
      description: Supplier identifier.
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: ../../components/schemas/LowStockItemDTO.yaml
    "400":
      description: Invalid request (e.g., missing supplierId)
    "401":
      description: Unauthorized
