```yaml
get:
  tags: ["Stock History"]
  summary: Stock history listing
  operationId: listStockHistory
  responses:
    '200':
      description: Paged stock history
      content:
        application/json:
          schema:
            $ref: ../../components/schemas/PagedStockHistoryDTO.yaml
    '400':
      description: Invalid request

```
