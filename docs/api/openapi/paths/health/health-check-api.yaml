get:
  tags:
    - Health
  summary: Check Oracle DB connectivity
  description: >
    Executes a simple "SELECT 1 FROM DUAL" to verify the database is reachable.
  responses:
    '200':
      description: Database is reachable
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/HealthStatus.yaml'
    '503':
      description: Database is not reachable or query failed
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/HealthError.yaml'

