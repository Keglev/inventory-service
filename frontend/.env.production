# Environment Configuration - Production

# ---- API ----
# Absolute URL to the production backend API (Fly.io deployment).
# Must be reachable from the browser; ensure CORS allows the frontend origin.
VITE_API_BASE=https://inventoryservice.fly.dev
