# Environment Configuration - Production

# ---- API ----
# Absolute URL to the production backend API (Hetzner deployment behind Caddy).
# Must be reachable from the browser; ensure CORS allows the frontend origin.
VITE_API_BASE=https://api.smartsupplypro.de
