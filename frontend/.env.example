# Environment Configuration Template - Frontend
#
# REQUIRED ENVIRONMENT VARIABLES:
# - VITE_API_BASE: Backend API endpoint URL (must be prefixed with VITE_ for browser exposure)
#   Use relative path '/api' for local development with proxy, or absolute URL for production.

# API endpoint for frontend consumption
VITE_API_BASE=/api
