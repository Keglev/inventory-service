# Environment Configuration - Development
#
# REQUIRED ENVIRONMENT VARIABLES:
# - VITE_API_BASE: Routes through Vite proxy configured in vite.config.ts

# API endpoint: routed through Vite development server proxy
VITE_API_BASE=/api
